# Solidity Starter (Hardhat)

## Setup
1. Install dependencies:
   npm install
2. Copy `.env.example` to `.env` and fill in RPC + key values.

## Compile
npm run compile

## Test
npm test

## Deploy (Sepolia)
npm run deploy
